import { Canvas, Meta } from "@storybook/blocks";
 import Example from "./example.tsx";
 import * as ButtonCustomProps from "./MyButton.stories.tsx";

 <Meta of={ButtonCustomProps} title="ButtonCustomProps" />

 # CustomButton

 Componente de botón con diferentes especializaciones.

 #### Example

 <Canvas of={CustomButton.Primary} />

 ## Usage

 ```ts
 import {ButtonCustomProps} from 'milibreria';

 function Example(){
  return (
      <ButtonCustomProps
        text: "Button primary",
        txtcolor: 'black',
        bgcolor: 'red',
        disabled: false,
        size: "small",
        onClick: () => alert("Button Primary"),
      />
  );
 };

 export default Example;
 ```

#### Example

 <Canvas of={ButtonCustomProps.Secondary} />

 ## Usage

 ```ts
 import {ButtonCustomProps} from 'milibreria';

 function Example(){
  return (
       <ButtonCustomProps
         text: "Button secondary",
         txtcolor: 'white',
         bgcolor: 'pink',
         disabled: false,
         size: "small",
         onClick: () => alert("Button Secondary"),
       />
   );
  };